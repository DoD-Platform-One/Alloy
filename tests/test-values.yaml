bbtests:
  enabled: true

alloy:
  # -- Resource requests and limits to apply to the Grafana Alloy container.
  resources:
    limits:
      cpu: 2000m
      memory: 4000Mi
    requests:
      cpu: 1000m
      memory: 2000Mi

k8s-monitoring:
  configValidator:
    enabled: true

  configAnalysis:
    enabled: true

autoRollingUpgrade:
  enabled: false

  test:
    enabled: true
